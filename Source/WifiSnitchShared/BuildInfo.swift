import Foundation

/// Build-time version information shared by the app and CLI.
public enum BuildInfo {
  /// The application version embedded at build time.
  public static let appVersion = "dev"
}
